# Unit Simple COnverter application with GUI

To run the application press run and input the number in one field, then press convert. To reset press clear.

To run.
> java --module-path (your java fx lib directory.) --add-modules javafx.controls -jar UnitConverter.jar

UML:

![](https://github.com/OOP2020/pa3-Noboomta/blob/master/uml.png)